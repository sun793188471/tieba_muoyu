# 更新日志

## v2.0.5 - 2026-07-27

### 修复

- 真实贴吧隐藏渲染页的媒体 `src` 仍为通用 icon 占位图，导致已有的 49 个 `dota2` 图片节点被全部当装饰图过滤

### 调整

- Excel 图片预览优先读取 `data-original` 等懒加载地址，再回退 `src`；高清查看仍优先使用 `bpic`

## v2.0.4 - 2026-07-27

### 修复

- 部分油猴隔离环境拒绝读取 `window.frameElement`，导致脚本在注册 `R` 快捷键前中断初始化

### 调整

- 内部贴吧渲染页改用专用查询参数标记，避免依赖沙箱窗口代理属性

## v2.0.3 - 2026-07-27

### 修复

- 从贴吧首页切换到底部贴吧 Sheet 后，静态 HTML 没有执行贴吧媒体初始化脚本，导致帖子列表图片全部缺失

### 调整

- 内部贴吧 Sheet 在静态响应有帖子但无图时，改为从同源隐藏页面读取渲染完成的列表 DOM
- 保留 `E` 隐藏图片语义：隐藏时显示 `[图片]` 占位，关闭隐藏后展示缩略图

## v2.0.2 - 2026-07-27

### 修复

- 进入具体贴吧后 Excel 帖子列表没有采集帖子图片
- `R` 关闭时 `E` 仍会隐藏贴吧原生图片
- 吧页图片查看器只使用裁剪缩略图，未优先读取节点提供的 `bpic` 原图
- 旧 GBK 百分号编码的贴吧链接导致 Sheet 标签显示乱码

### 调整

- `R` 成为内容与展示托管总开关，关闭时恢复过滤、折叠、标记、链接和图片地址等原生状态
- `Q`、`E`、`D` 仅在 Excel 模式开启时允许切换
- 帖子 Sheet 顶栏增加“↗ 打开原贴”入口，并使用新标签页打开对应贴吧帖子

## v2.0.1 - 2026-07-27

### 修复

- “隐藏头像”设置被快捷键运行时状态覆盖的问题
- 楼中楼 `.lzl_p_p` 头像未被隐藏的问题
- 设置面板未展示快捷键切换后的实际状态

### 改进

- Excel 顶栏增加“⚙ 设置”按钮，可直接呼出基础设置

## v2.0.0 - 2026-07-27

### 新增

- Excel 模式内联帖子 Sheet 与帖子分页
- 表格图片的 `imgsa`、`imgsrc` 高清原图候选及失败回退
- 19 项设置/参数的自动化回归测试和真实贴吧 HTML 冒烟测试
- 油猴脚本自动更新地址

### 修复

- 帖子图片被链接包裹时无法在 Excel 正文展示
- 图片查看器只加载 580px 缩略图
- Excel 帖子链接跳出当前页面或重复重绘后点击失效
- 采集正文时删除原页面楼中楼节点
- 设置面板首次导入导出、扩展坞首次打开设置无效
- 快捷键显式关闭状态被基础设置重新覆盖
- 异常编码贴吧链接中断 Excel 初始化
- 表格、关键字和黑名单管理的 HTML 注入风险

### 调整

- 高级数值设置增加有效范围，非法历史值自动修正
- 重置配置同时清理关键字、黑名单和快捷键运行时状态
